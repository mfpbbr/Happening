<div>
<%= image_tag(@restaurant.image_url) %>
<h3><%= @restaurant.title %></h3>
<div class="externalurl"><%= link_to @restaurant.url, @restaurant.url %></div>
<dl>
  <dt class="icon-tags"></dt>
  <dd><%= @restaurant.category_list %></dd>
  <dt><%= image_tag(@restaurant.rating_image_url) %></dt>
  <dd><%= @restaurant.review_count %> reviews</dd>
  <dt class="icon-map-marker"></dt>
  <dd><%= @restaurant.address %></dd>
  <dt>Distance:</dt>
  <dd class="distance"><%= @restaurant.distance %></div> 
</dl>
<%= render partial: 'likes/like_form', locals: { like_object: find_like(current_user, @restaurant), likeable: @restaurant } %>
<%= render partial: 'comments/comment_form', locals: { commentable: @restaurant } %>
<br />
<ul class="comments">
  <%= render @restaurant.comments %>
</ul> 
</div>

